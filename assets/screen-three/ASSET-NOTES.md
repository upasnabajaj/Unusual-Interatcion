# Screen 3 artwork preparation

Original Figma layer: file `vfHLFM7rPdrIF8UjVrL1Yt`, screen `272:150`, background node `304:211`.

- `environment.png`: untouched original Figma background layer. It includes six pre-engraved boulders.
- `dormant-environment.png`: built-in ImageGen precise-object edit of that background. Existing architecture and camera composition are preserved, engraved surfaces are blanked, and two matching small stones are added. Dimensions: 1672 × 941.
- `dialogue.svg`: original separate Figma dialogue outline.

The edited file is a background layer, not a flattened website screenshot. The fairy, dialogue text, moving light, eight hit regions and discovered flower patterns are rendered independently by the application. Revealed flower artwork comes directly from the selected Screen 2 assets.

## Built-in image-edit prompt

Precise object edit of the supplied original background for an interactive website. Preserve the EXACT wide 1672x941 composition, architecture, arches, camera, ivy, stone floor, dark lighting and all six existing boulders in exactly their positions. Remove ONLY the pale flower engravings and engraved rings from all six boulder tops, restoring realistic unmarked dark rock texture. Add exactly TWO additional smaller matching dormant unmarked flat-topped boulders: one in the left middle ground on floor near pixel (200,530) and one in right middle ground near (1400,430). There must be EIGHT total flat-topped exploration stones. All eight should be natural rocky roughly cut dark boulders with subtle ivy at base, no engravings, no symbols, no numbers, no text. Do not add fairy, dialogue, UI, lighting pool or particles. This is only the background asset; moving lighting and interactions are implemented separately in code. Keep all unaffected pixels and the very dark palette as close to input as possible. Output landscape same aspect ratio.

The two new stones were placed slightly differently by the image edit; live hit regions were aligned to their actual output positions after visual inspection.
